# Aries Game System

基于 Hyperledger Aries 的多账号游戏系统，支持分布式身份认证和可验证凭证管理。

## 功能特性

- 🎮 **实时多人在线游戏**：基于 WebSocket 的实时游戏体验
- 🆔 **自定义 DID 方法**：实现 `did:player` 身份认证系统
- 🏆 **W3C 凭证管理**：支持游戏成就和资质凭证的颁发与验证
- 💬 **DIDComm 消息**：玩家间分布式消息通信
- 🎯 **剧本系统**：管理员可自定义游戏场景和任务
- 🔐 **安全钱包**：本地密钥和凭证管理

## 技术架构

```
┌─────────────────┐    WebSocket    ┌─────────────────┐
│  HTML5 Canvas   │◄──────────────►│   Go Server     │
│     Client      │                 │                 │
├─────────────────┤                 ├─────────────────┤
│ • Game Engine   │                 │ • Game Logic    │
│ • Wallet        │                 │ • DID Service   │
│ • UI Manager    │                 │ • VC Service    │
│ • Network       │                 │ • Script Engine │
└─────────────────┘                 └─────────────────┘
                                              │
                                    ┌─────────────────┐
                                    │ Aries Framework │
                                    │ • DID Registry  │
                                    │ • VC Manager    │
                                    │ • Crypto        │
                                    └─────────────────┘
```

## 快速开始

### 环境要求

- Go 1.21+
- 现代浏览器（支持 WebSocket 和 Canvas）

### 安装依赖

```bash
make deps
```

### 运行开发服务器

```bash
make dev
```

访问 http://localhost:8080 开始游戏。

### 构建生产版本

```bash
make build
make run
```

## 项目结构

```
aries-game-system/
├── client/                 # HTML5 客户端
│   ├── src/
│   │   ├── engine/        # Canvas 游戏引擎
│   │   ├── network/       # WebSocket 通信
│   │   ├── ui/            # 用户界面
│   │   └── wallet/        # 钱包管理
│   ├── assets/            # 游戏资源
│   └── index.html         # 主页面
├── server/                # Go 服务器
│   ├── cmd/               # 服务器入口
│   └── internal/
│       ├── game/          # 游戏逻辑
│       ├── did/           # DID 身份认证
│       ├── vc/            # VC 凭证管理
│       └── script/        # 剧本系统
├── aries-framework-go/    # Aries 框架源码
└── docs/                  # 文档
```

## 开发指南

### DID 身份系统

系统使用自定义的 `did:player` 方法：

```
did:player:gameID:playerID
```

### 凭证类型

支持多种游戏凭证：
- 成就凭证（Achievement Credential）
- 等级凭证（Level Credential）
- 技能凭证（Skill Credential）
- 道具凭证（Item Credential）

### API 接口

- `POST /api/did/create` - 创建玩家 DID
- `GET /api/did/resolve` - 解析 DID 文档
- `POST /api/vc/issue` - 颁发凭证
- `POST /api/vc/verify` - 验证凭证
- `WS /ws/game` - 游戏 WebSocket 连接

## 贡献指南

1. Fork 项目
2. 创建功能分支 (`git checkout -b feature/AmazingFeature`)
3. 提交更改 (`git commit -m 'Add some AmazingFeature'`)
4. 推送到分支 (`git push origin feature/AmazingFeature`)
5. 打开 Pull Request

## 许可证

本项目采用 MIT 许可证 - 查看 [LICENSE](LICENSE) 文件了解详情。
